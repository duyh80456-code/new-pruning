Dropped from the queue after the lh session:

  n_jeffreys_narrow  L was the same reasoning applied to D. It projected
                     to 73.58 and landed at 72.74, below the branch it was
                     built from. Weighting a term by width does not keep
                     the half of a per-width table that looked good.

  e_kl_pair          Separates symmetry from coupling inside F. F ties A
                     at +0.03, inside the rounding floor, so there is no
                     effect left to attribute.

  g_flat_cost        Would confirm the classifier cost matrix contributes
                     nothing. H already took the metric from the teacher's
                     confusions instead, moved C by +0.42, and still fell
                     0.36 short of A. The tier is closed by results rather
                     than by a control.

  o_temp4            Temperature was a diagnosis for six branches landing
  p_jeffreys_temp4   inside 0.8 points of each other. K then cleared A by
                     0.75 without it, so the diagnosis is no longer what
                     stands between the project and a result.

The configs stay in apps/. Any of them runs from kaggle_kd_kj.ipynb by
editing BRANCHES.
